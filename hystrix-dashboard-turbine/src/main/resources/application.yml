spring:
  application:
    name: hystrix-dashboard-turbine
server:
  port: 8763
turbine:
  appConfig: user-provider,product-provider
  aggregator:
    clusterConfig: default
  clusterNameExpression: new String("default")

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/